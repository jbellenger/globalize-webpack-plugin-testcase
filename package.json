{
  "devDependencies": {
    "cldr-data": ">=25",
    "globalize": ">=1.2.0-a <2.0.0",
    "html-webpack-plugin": "^2.28.0",
    "globalize-webpack-plugin": "^0.3.10",
    "webpack": "^2.2.0"
  },
  "scripts": {
    "start": "npm run build && open meta-index.html",
    "build": "webpack --config webpack-config.js",
    "prebuild": "rm -fR dist"
  }
}
